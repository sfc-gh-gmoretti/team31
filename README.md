# team31

Open `index.html` to get started — click it in your file browser, or from the terminal run:

```bash
open index.html
```

Meeting notes live in the [`meeting-notes/`](meeting-notes/) directory — one file per stakeholder.
